services:
  - type: web
    name: extrator-leilao
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn extrator_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
